<UserControl
    x:Class="View.View.Patient.PatientGridView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:local="clr-namespace:View.View.Patient"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <dxg:GridControl
            AutoGenerateColumns="AddNew"
            EnableSmartColumnsGeneration="True"
            ItemsSource="{Binding Patients}"
            SelectedItem="{Binding SelectedPatient}">
            <dxg:GridControl.Columns>
                <dxg:GridColumn Width="3*" FieldName="FirstName" />
                <dxg:GridColumn Width="3*" FieldName="LastName" />
                <dxg:GridColumn Width="3*" FieldName="BirthDate" />
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView AutoWidth="True" BestFitModeOnSourceChange="VisibleRows" />
            </dxg:GridControl.View>
        </dxg:GridControl>
    </Grid>
</UserControl>
